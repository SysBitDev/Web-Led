#ifndef HTTP_H
#define HTTP_H

/// @brief Starts the web server to handle HTTP requests.
void start_webserver(void);

/// @brief Stops the running web server.
void stop_webserver(void);

#endif // HTTP_H
